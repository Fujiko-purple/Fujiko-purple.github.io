---
title: 39. 二叉树的最大深度
date: 2026-07-27
category: algorithm
---

# 二叉树的最大深度（LeetCode 104）

## 题目
给定一个二叉树，找出其最大深度。最大深度是从根节点到最远叶子节点的最长路径上的节点数。

```
    3
   / \
  9  20
    /  \
   15   7

最大深度 = 3
```

## 核心思想
当前节点的深度 = `max(左子树深度, 右子树深度) + 1`，自底向上递归。

## 关键步骤
1. 空节点返回 0
2. 递归计算左子树深度、右子树深度
3. 返回 `Math.max(left, right) + 1`

## 时空复杂度

| 项目 | 复杂度 | 推导 |
|------|--------|------|
| 时间 | O(n) | 每个节点访问一次 |
| 空间 | O(h) | 递归栈深度 = 树高，最坏 O(n)，平均 O(log n) |

## 模式归类：二叉树-深度递归（自底向上）

## Java 代码

### 递归（自底向上，推荐）
```java
public int maxDepth(TreeNode root) {
    if (root == null) return 0;
    int left = maxDepth(root.left);
    int right = maxDepth(root.right);
    return Math.max(left, right) + 1;
}
```

### 自顶向下
```java
class Solution {
    int max = 0;

    public int maxDepth(TreeNode root) {
        dfs(root, 0);
        return max;
    }

    void dfs(TreeNode node, int depth) {
        if (node == null) return;
        depth++;
        if (node.left == null && node.right == null) {
            max = Math.max(max, depth);
        }
        dfs(node.left, depth);
        dfs(node.right, depth);
    }
}
```

### BFS（层序遍历，数层数）
```java
public int maxDepth(TreeNode root) {
    if (root == null) return 0;
    Queue<TreeNode> queue = new LinkedList<>();
    queue.offer(root);
    int depth = 0;
    while (!queue.isEmpty()) {
        int size = queue.size();
        for (int i = 0; i < size; i++) {
            TreeNode node = queue.poll();
            if (node.left != null)  queue.offer(node.left);
            if (node.right != null) queue.offer(node.right);
        }
        depth++;
    }
    return depth;
}
```

## 易错点
1. 空节点返回 0 不是 1，否则深度多算一层
2. `Math.max` 不是 `Math.Max`（Java 大小写敏感）
