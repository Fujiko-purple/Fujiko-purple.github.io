# 📕 个人使用手册（速查）

> 给自己看的备忘：想干什么，就抄哪段命令。原理和规范见 [README.md](README.md)。

---

## 🖊️ 写一篇新笔记（最常用）

**方式一：用 Claude Code（推荐）**

```
/new-note linux 防火墙配置     ← 自动建好文件，告诉你路径
（用编辑器写正文）
/publish                      ← 一键构建检查+提交+推送+验证上线
```

**方式二：纯手动**

1. 在对应分类目录新建文件：`content/notes/linux/防火墙配置.md`
2. 开头抄这段（date 改成今天）：

```markdown
---
category: linux
date: 2026-07-26
---
# 防火墙配置

正文……
```

3. 推上去（推完等 1-2 分钟自动上线）：

```bash
git add .
git commit -m "笔记: 防火墙配置"
git push
```

现有分类：`linux` `python` `database` `algorithm`（category 填哪个，笔记就归到哪组）

---

## 🧮 同步算法刷题笔记

在 `Desktop\秋招培训\每日算法` 里刷完题、写完 `笔记.md` 之后，对 Claude Code 说：

```
同步算法笔记        （或 /sync-algo）
```

它会自动找出站点里还没有的新题目，加好 front matter 导入并发布上线。手动拷贝不需要。

---

## ✏️ 修改已有笔记

改完 `content/` 里的 md 文件，然后同上推送三连（或 `/publish`）。就这样，别的什么都不用动。

---

## 👀 发布前先在本地看效果（可选）

```bash
node build.mjs
```

然后双击打开 `dist/index.html`。看完不满意就继续改，`dist/` 不用提交（git 会自动忽略它）。

---

## 📂 加一个新分类（比如想加 docker 笔记）

```
在 Claude Code 里直接说：/new-note docker docker网络
```

它会自动建 `content/notes/docker/` 目录和 `_index.md`，并把中文名加进 `build.mjs`。手动做的话参照 README「场景二」。

---

## 🗂️ 加一个全新分区（和"学习笔记"平级，低频）

要同步 3 个地方，容易漏——直接让 Claude Code 做：

```
说：帮我加一个"日记"分区
```

手动步骤见 README「场景三」。

---

## 🌐 常用地址

| 地址 | 用途 |
|------|------|
| <https://fujiko-purple.github.io/> | 线上站点 |
| <https://github.com/Fujiko-purple/Fujiko-purple.github.io> | 仓库 |
| <https://github.com/Fujiko-purple/Fujiko-purple.github.io/actions> | 部署状态（绿勾=已上线，红叉=失败） |

---

## ❓ 出问题了

| 现象 | 处理 |
|------|------|
| 推送后线上没更新 | 等 1-2 分钟再刷新（Ctrl+F5 强制刷新）；还不行去 Actions 页看是否红叉 |
| 刚修的问题看起来还在 | 浏览器可能在用 10 分钟内的缓存旧页面。**用无痕窗口（Ctrl+Shift+N）测一次**：无痕里好了=缓存问题，等一会或 Ctrl+F5；无痕里还坏=真没修好 |
| Actions 红叉 | 打开红叉那条看哪步失败，或直接把链接丢给 Claude Code |
| 笔记里格式显示不对 | 解析器只支持部分 Markdown 语法（见 README），嵌套列表用 **2 个空格**缩进 |
| 图片想插进笔记 | 图片放 `site/assets/` 下，笔记里写 `![说明](../../assets/图名.png)`（notes 下二级目录时是两层 `../`） |

---

## 🚫 三条红线（记住就不会出事）

1. **`dist/` 永远不用管**——它是机器生成的，git 也不收
2. **GitHub 仓库设置里的 Pages 部署源不要动**（保持 GitHub Actions 模式）
3. **不要装 npm 依赖**——这个项目零依赖是刻意的

---

*配套：项目规范和原理 → [README.md](README.md)；AI 一键命令 → `/new-note`、`/publish`*
